# Manual Verification

Purpose: acceptance procedures and expected observations for the sidebar and account flows. These are checklists, not evidence that a check has passed. Product behavior belongs in [Features](features.md); pending checks remain in [TODO](../TODO.md). Build and package instructions are in [Development](development.md).

## Preview verification

Every frontend change must be checked in the preview page at sidebar/container widths **200, 240, 280, 340 and 420px**, in both English and Simplified Chinese. There must be no horizontal scroll bar; names, emails, directories, version values and banner text must wrap or be ellipsized; button groups must not overlap text. Check both sides of the 340px container-query breakpoint and include screenshots and assertions in the report.

Keep the browser full-screen and at 100% zoom. Adjust the sidebar or preview container width, not the browser viewport or page zoom.

During preview verification, every agent (the main agent and each subagent independently) opens exactly one browser tab for the task and reuses it with `navigate_page` (reload or change URL parameters). Do not share tabs between agents or call `new_page` repeatedly. Include this rule in any delegated frontend verification task. When finished, close the tab you opened and stop the preview static server by its PID. Never kill processes by name; only clean up processes started for the current task.

A static preview verifies layout, not the real editor integration or account switching. Report those checks separately. The real-account steps below are performed by the user; a preview does not authorize writing account data or restarting a WSL server.

## WSL setup and safety

`npm test` covers the pure modules only; for changes affecting UI behavior, verify the affected flows manually in a **WSL window**. Two ways:

- **Install the vsix**: `npm run package`, then in the WSL window run `Extensions: Install from VSIX...`, install and reload.
- **Extension Development Host**: open this repository in a WSL window and press F5 (the "Run Extension" configuration). `.vscode/tasks.json` runs `npm run build` first and then starts the Extension Development Host window. For frontend problems run `Developer: Open Webview Developer Tools` in the development host to see the console (e.g. CSP errors). If the vsix of this extension is installed, disable it in the development host first to avoid duplicate command and view registrations. If F5 never attaches (VS Code 1.139, "Could not connect to debug target"), see [Development: F5 troubleshooting](development.md#1-f5-does-not-load-the-extension-in-vs-code-1139-js-debug-attach-regression).

This checklist creates and deletes directories under the real `~` and is performed by the user personally; agents must never perform its write or delete steps in the real home directory themselves. Using test accounts is recommended to avoid deleting real account directories by mistake. Checks:

## Claude and shared panel

1. **Activation**: the "PlanSwap" icon appears in the activity bar; when opened there is exactly one "PlanSwap" view with the Claude / Codex tab bar at the top; the Claude page shows the "All accounts" list (current account in the first row and highlighted) (including the `default` row, correct count badge), the "Tools" row and the add-account section at the bottom; the footer toolbar and its separate version line are pinned to the bottom; the title bar only has the refresh button; icons render (codicon font loaded); the Webview developer tools console shows no CSP errors; the status bar shows `Claude: default` (or the current account). Switch between light and dark themes and confirm the panel colors follow.
2. **Tab switching**: click the Codex tab and confirm the page switches and the selected state is highlighted; after closing and reopening the panel (or reloading) the last selected tab is still active; run "Add Account (Focus Sidebar Input)" from the Command Palette and confirm it switches back to the Claude tab and focuses the input; run "Add Codex Account" and confirm it switches to the Codex tab and focuses its input; the inputs of the two pages do not affect each other.
3. **Email and plan**: signed-in Claude account rows show the email and plan (e.g. `Max 20x`, `Pro`), and the first line of the status bar tooltip is "email · plan"; when `~/.claude.json` has no `organizationType`, no plan is shown and no error occurs.
4. **Rename**: confirm the pencil sits right after the name (after the link badge), is hidden until the row is hovered or focused, stays on the last line of a long wrapped name, and is no longer in the button group; confirm the `default` row and the external-directory row have no pencil button; double-click the pencil and confirm it only starts renaming and does not switch. Click the pencil icon of a named row (e.g. `test1`) and confirm the name turns into an input prefilled with `test1`; enter blank / more than 32 characters / another account's name / another account's display name / the external-directory name ("External directory" and "外部目录"), confirm the reason is shown in red inline and nothing changes (clicking elsewhere keeps the typed text and the reason); Esc cancels and restores the row; confirm the card height does not change when entering or leaving edit mode (single-line and wrapped names, narrow and wide panels) and a check-mark save button follows the input; change the name and click elsewhere, and separately click the save button, and confirm both save; click elsewhere without changes and confirm it just leaves edit mode; enter `work-main` and press Enter, confirm the row, the status bar (when `test1` is current) and the Command Palette QuickPick all show `work-main`, the directory `~/.claude-test1` is unchanged, and switching and the terminal command still use the original directory; type `work-main` in the add input and confirm it is rejected as "same as an existing account's display name"; then enter `test1` and press Enter to restore the name; remove an account that has an alias, add an account with the same name again and confirm the alias was cleared. Repeat on the Codex page and confirm the aliases of the two pages are independent and both pages may use the same alias.
5. **Add (shared and independent)**: type invalid names in the bottom input (e.g. `a b`, `default`, an existing account name) and confirm the input turns red, the help line shows the reason and the add button is disabled. Confirm the checkbox "Link to the default account's settings and history" is checked by default, the help line changes with it, and its state survives a list refresh. With the checkbox checked, type `test1` and press Enter: the input is cleared, `test1` appears with the `link` badge, `~/.claude-test1` was created (mode 0700), and `settings.json`, `CLAUDE.md`, `history.jsonl`, `projects`, `sessions`, `file-history`, `todos`, `agents`, `commands`, `hooks`, `rules`, `ide` and the other entries of design.md 6.7 are absolute symlinks into `~/.claude`; `skills` / `plugins` are real folders with one link per default child (none for `synced` / `.trash`); `.credentials.json` is not there; `~/.claude-test1/.claude.json` (0600) has the default account's `mcpServers` and the per-project trust keys, and `CLAUDE_CONFIG_DIR=~/.claude-test1 claude mcp list` lists the servers; existing files in `~/.claude` are unchanged (missing shared entries were created empty). Put `apiKeyHelper` into `~/.claude/settings.json` temporarily and add `test3`: a warning says `settings.json` was not linked for safety and `~/.claude-test3/settings.json` does not exist. Uncheck the checkbox and add `test2`: no badge, a "Link to the default account: …" button on the row, and `~/.claude-test2` has regular copies of `settings.json` (keys of design.md 6.2 stripped), `CLAUDE.md`, `agents` / `commands` / `output-styles` / `hooks` / `rules`, the `skills` children and the MCP servers, and no links. Run "Add Account (Focus Sidebar Input)" from the Command Palette and confirm it opens the panel and focuses the input.
6. **Switch**: click the switch button of the `test1` row and confirm a modal "Switch the Claude account to test1? …" appears; cancel it and confirm nothing changed, then switch again and click "Switch" (also verify that double-clicking the row and pressing Enter after focusing it with Tab show the same modal, that a single click does nothing, and that the Command Palette switch has no modal); confirm the remote Machine setting `claudeCode.environmentVariables` contains `CLAUDE_CONFIG_DIR` with the absolute path and other entries are kept; no notification is shown, and a "Switched to test1" banner appears at the top of the panel; the current-account highlight and the status bar update; a new session in the official panel shows its sign-in screen. Click the banner's close button and confirm the banner disappears without reloading; switch again and click "Reload Window" and confirm the window reloads. Collapse or hide the panel and switch from the Command Palette; confirm a notification with a "Reload Window" button is shown instead. Switch back to `default` and confirm the entry is removed. Replace one link of the shared `test1` by hand (e.g. delete `~/.claude-test1/todos`), add an MCP server to the default account, then switch to `test1`: the link is re-created and the server appears in `~/.claude-test1/.claude.json` before the switch; replace `~/.claude-test1/CLAUDE.md` by a regular file and switch again: a warning lists `CLAUDE.md` as kept by the account, and the switch still happens. Replace `~/.claude-test1/history.jsonl` by a regular file with an extra line and switch again: the line is appended to `~/.claude/history.jsonl` and the link is re-created without a warning.
7. **Terminal sign-in**: click the terminal button of the `test1` row, confirm the terminal is named `Claude (test1)` and the command sent is `env CLAUDE_CONFIG_DIR='<absolute path>' claude`; after signing in the row shows email and plan, and the "Not logged in" tag and the sign-in hint disappear.
8. **Remove**: click the remove button of the `test1` row and confirm the row turns into an inline confirmation; click Cancel and the row is restored; click remove again and confirm, then a system modal asks whether to delete the directory; confirm the directory is deleted. Also verify: removing `test1` while it is current is not possible (no remove button); removing from the Command Palette first shows a modal confirmation; the default row and the external-directory row have no remove button; refused paths (symlinked directory, default directory) are not deleted. Delete the directory of a shared account and confirm only its links and own files are gone: `~/.claude/projects`, `history.jsonl`, `settings.json` and the other shared content are intact.
9. **External directory**: set `CLAUDE_CONFIG_DIR` by hand to an unregistered directory and confirm an "External directory" row (avatar `?`) appears, is current and pinned to the first row, can be switched away from, has a terminal button ("Log in" button when signed out) and no remove button. Also confirm that no current account ever has a remove button.
10. **Auto-discovery**: create `~/.claude-foo` by hand, then click refresh or reload and confirm it is registered automatically; remove that account keeping the directory, click refresh and confirm it is not registered again.
11. **Errors**: introduce a syntax error into the remote settings.json and switch; confirm a reason is shown instead of a silent failure; make the target path of an add a regular file with the same name and confirm the help line shows "Failed to create account directory" and nothing is registered.
12. **Tools row and Re-link**: on each page the "Tools" row has four buttons (`CLAUDE.md` / `AGENTS.md`, Settings, Re-link, Update CLI). Click the `CLAUDE.md` / `AGENTS.md` button and confirm the current (effective) account's global rules file opens; with the file missing, confirm a modal asks whether to create it and, after confirming, an empty file (0600) is created and opened. Click the settings button and confirm the Settings UI opens filtered by `claudeCode.` / `chatgpt.`. With only independent accounts, click "Re-link" and confirm "No linked Claude accounts to re-link." and that nothing changed. With shared accounts: delete one of their links, replace another account's `todos` link by a real folder, remove an MCP server from the default account, then click "Re-link": the deleted link is re-created, the removed server disappears from every shared account's `.claude.json`, one warning reads "Re-linked N linked Claude account(s) to the default account. Needs attention: <name>: kept the account's own: todos", the real folder and every independent account are untouched. Make one shared account's `.claude.json` invalid JSON and confirm it is listed with its reason and left unchanged. Repeat on the Codex page (also while Codex switching is not enabled, where the "Tools" row is still shown). Run "Re-link Accounts to the Default Account" from the Command Palette and confirm it first asks to pick Claude Code / Codex.
   Verify the Update CLI action with a mocked terminal: Claude sends `claude update`, Codex sends `env -u CODEX_HOME codex update`; the terminal is shown, its name is localized, and the selected account and extension-host environment stay unchanged. Check the Codex action while switching is disabled too. Do not run a real CLI update as an automated test.
13. **Footer toolbar, version card and extension version**: the footer toolbar stays pinned at the bottom while the page content scrolls above it, on both tabs. Click "Show CLI and extension versions" and confirm a card opens above the toolbar with four items (Claude Code CLI, Claude Code extension, Codex CLI, Codex extension), each with its label on one line and its value in monospace on the next; a missing CLI shows "Not found"; clicking the button again or the close button collapses it. "Reload Window" and "Restart Extension Host" act immediately without confirmation; "Restart WSL Server" shows the modal confirmation (cancel it; see the Codex warning below). Run "Show CLI and Extension Versions" from the Command Palette and confirm a read-only list is shown. Confirm the User guide and Star buttons open `https://github.com/n2ns/planswap#readme` and `https://github.com/n2ns/planswap`, respectively. Confirm a separate small line below the toolbar displays `v<version>` matching the packaged manifest.
14. **Widths**: at panel widths 200 / 240 / 280 / 340 / 420px, in both languages, confirm there is no horizontal scroll bar, long names / emails / directories / version values / banner text / the shared checkbox label wrap, the shared badge stays next to the name without overlapping the current badge, button groups never overlap text, the "Tools" row buttons wrap to a new line when needed, and the layout switches correctly at the 340px breakpoint.
15. **Language**: set `planswap.language` to `en`, then `zh-cn`, then `auto`; confirm each time without reloading that the panel (tabs, titles, banners, buttons, tooltips, placeholders, add-section help text, footer toolbar titles, version card, disabled Codex page), the status bar tooltip, notifications, modal dialogs and QuickPick lists switch language immediately, and that an open inline rename or typed add-input text is not broken by the re-render. With `auto`, confirm the language follows VS Code's display language. Confirm that command titles, categories and the view name follow VS Code's display language only (not the setting). Confirm the rc marker block written by "Enable" is identical in both languages.

16. **Share an independent account**: with the independent `test2` (created in step 5, signed in and with a few sessions, a `history.jsonl` and a `CLAUDE.md` that differs from the default one), confirm the current row and the shared rows have no "Link to the default account: …" button; while `test2` is current, confirm it has none either. Run `claude` in a terminal as `test2`, switch to another account, click the button on `test2` and confirm the modal; confirm "Claude Code is still running with account test2…" appears and nothing moved. Exit that session and share again: the notification summarizes moved / dropped / kept-both / backed-up files; `~/.claude-test2/projects` and the other shared entries are now links, its sessions show up under `~/.claude/projects`, its history lines were appended to `~/.claude/history.jsonl`, `~/.claude-test2/CLAUDE.md.independent-backup` holds its own rules (when `~/.claude` had no `CLAUDE.md`, the account's file was moved there instead), differing files sit next to the default ones as `<name>.from-test2`, no existing default file was overwritten, and `.credentials.json` and the sign-in stayed in `~/.claude-test2`. The row now has the `link` badge. Cancel the modal on another account and confirm nothing changed.

17. **Unlink a shared account**: with the shared `test1`, confirm the current row and the independent rows have no "Unlink from the default account: …" button, and that `test1` has none while it is current. Switch away, run `claude` in a terminal as `test1`, click the button and confirm the modal: "Claude Code is still running with account test1…" appears and nothing changed. Exit the session and unlink again: the notification reads "test1 is now independent: removed N link(s), copied …"; `~/.claude-test1` has no links left, regular copies of `settings.json` (keys stripped), `CLAUDE.md`, the config folders and the `skills` children, its `.claude.json` has the MCP servers, and no `projects`, `sessions` or `history.jsonl`; `~/.claude/projects`, `history.jsonl` and every other default file are unchanged; `.credentials.json` stayed. The row lost the badge and shows the link button; linking it again works. Replace one link by a regular file before unlinking and confirm that file is kept.

## Codex

For changes to shell configuration or automatic server restart, the user also verifies the section 5 design acceptance criteria with a temporary empty account directory: after the confirmed switch, the old server, pty host and integrated terminals have exited; after reconnecting, the new extension host's `process.env.CODEX_HOME` equals the selected state-file value and new Codex processes inherit that value. Check the enable self-check and return-to-default steps below as well. These observations do not authorize an agent to signal the server.

**Warning: in Antigravity and VSCodium, a Codex switch and "Restart WSL Server" really restart the editor's WSL server, which disconnects every WSL window, closes integrated terminals and ends the current Claude Code session.** The following steps are also performed by the user personally; agents must never perform any step that writes to the real home directory, deletes directories or restarts the server.

1. **Tab page**: the container title and the view name are both "PlanSwap" (the sidebar title is shown only once); click the Codex tab at the top to switch to the Codex page; the title-bar refresh button works for both pages. While not enabled, the Codex page only shows the explanation and the "Enable Codex switching" button plus the "Tools" row; the add section is hidden.
2. **Enable pre-checks**: temporarily add a line `export CODEX_HOME=/tmp/x` to `~/.bashrc` and click enable; confirm the error lists the conflict and nothing is written; restore and try again. If `~/.bash_profile` exists and does not source `~/.bashrc`, confirm enabling is refused.
3. **Enable**: click "Enable Codex switching", confirm the modal shows the full marker block; after clicking "Write", the block in `~/.bashrc` is located before the `case $- in` guard and the block in `~/.profile` is at the end, and both files keep their permissions; the self-check passes and the Codex page switches to the account layout (the `default` row is current and has no pencil icon; every named account row has one); `bash -i -l -c 'echo $CODEX_HOME'` prints nothing (the state file is empty).
4. **Add (shared and independent)**: with the shared checkbox checked, type `test1` and confirm `~/.codex-test1` (0700) is created with the `link` badge; `config.toml`, `AGENTS.md`, `hooks.json`, `history.jsonl`, `session_index.jsonl`, the four `*.sqlite` thread databases (dangling until Codex creates them), `.tmp/rollout-maintenance.lock` (inside a real `.tmp` folder), `sessions` and the other folders of codex-design.md 8.6 are absolute symlinks into `~/.codex`; `skills` / `plugins/cache` are real folders with per-child links (none for `.system` / `openai-curated-remote`); `auth.json`, `memories` and `.tmp/rollout-compression.lock` are not linked; existing files in `~/.codex` are unchanged. Temporarily add a line `model_provider = "x"` to `~/.codex/config.toml`, add `test3` (shared) and confirm a warning says `config.toml` was not linked for safety and `~/.codex-test3/config.toml` does not exist; then uncheck the checkbox, add `test2` and confirm a "not copied" message appears and `~/.codex-test2` has no `config.toml` but regular copies of `AGENTS.md`, `hooks.json`, `rules` / `hooks` / `agents` / `themes` and the `skills` children, no links and no `auth.json`. Remove the temporary line again.
5. **Switch**: click the switch button of `test1` and check the modal confirmation text; after confirming, the state file contains the absolute path of `~/.codex-test1` with mode 0600; then every WSL window shows "Cannot reconnect", and after clicking "Reload Window" in each one, `test1` is current on the Codex page and there is no "takes effect after restart" banner; in a new terminal `echo $CODEX_HOME` prints that directory; the official Codex panel shows signed out (new directory) but lists the default account's sessions (shared). Delete a thread in the Codex panel so that Codex replaces the `session_index.jsonl` link by a regular file, then switch away and back (or click "Re-link"): the lines are merged back into `~/.codex/session_index.jsonl` and the link is re-created.
6. **Takes-effect-after-restart banner**: change the state file by hand to another registered directory and confirm the top of the Codex page shows "X selected; takes effect after restarting the server" with a "Restart server" button; change it back and the banner disappears.
7. **Terminal sign-in, email and plan**: click "Log in" on a signed-out row and confirm the terminal is named `Codex (test1)` and the command is `env CODEX_HOME='<absolute path>' codex login`; the terminal button of a signed-in row sends the command without `login`; the `default` row sends `env -u CODEX_HOME codex`. After a ChatGPT sign-in the row shows email and plan (e.g. `Plus`, `Pro`, `Team`); a directory signed in with an API key shows "API key" and no email; after changing `auth.json` by hand to invalid JSON the row shows "Logged in" without email and no error. Confirm that no raw token appears anywhere in the extension host log or the panel.
8. **Remove**: the current (effective) account row has no remove button; the Command Palette remove list contains neither the effective account nor the selected account; remove `test2` choosing to delete the directory and confirm it is deleted; run `codex` in an account directory so that its daemon is alive, try to delete that directory and confirm it is refused with a message that the daemon is still running. Delete the directory of a shared account and confirm `~/.codex/sessions`, `history.jsonl`, `config.toml` and the thread databases are intact.
9. **Switch back to default and disable**: switch back to `default`; after the restart `CODEX_HOME` is empty in a new terminal. Run "Disable Codex Account Switching" and confirm the marker blocks in both files (including the blank line added before the block in `~/.bashrc`) are removed exactly, the state file is deleted and the Codex page returns to the disabled page. Delete the end marker in `~/.bashrc` by hand and disable again; confirm an error is reported and the file is unchanged.
10. **Manual method**: close all windows, wait 5 minutes and reopen; confirm the connection works and `CODEX_HOME` follows the state file.
11. **VSCodium automatic restart**: in a VSCodium WSL window, switch the Codex account; confirm the modal names VSCodium ("Switching the Codex account restarts VSCodium's WSL server: …"), and after confirming every WSL window shows "Cannot reconnect", and after "Reload Window" the new account is current and `echo $CODEX_HOME` in a new terminal prints its directory. Repeat with "Restart WSL Server" and confirm the modal "Restart VSCodium's WSL server: …".
12. **VS Code manual guidance**: in a VS Code WSL window, switch the Codex account; confirm the modal reads "The new Codex account takes effect only after the WSL server restarts, which this editor cannot do automatically. Close all VS Code windows connected to this distro, wait a few seconds, then reopen them. Continue?", and after confirming the state file is written, no window disconnects, and no further warning is shown. "Restart WSL Server" (banner button, footer toolbar, Command Palette) shows the warning "This editor's WSL server cannot be restarted automatically. Close all VS Code windows connected to this distro, wait a few seconds, then reopen them." directly, without a modal. Close all VS Code windows connected to the distro, wait a few seconds, reopen, and confirm the new account is current and the "takes effect after restart" banner is gone.
13. **Share an independent Codex account**: with the independent `test2` (signed in, with sessions and its own `state_5.sqlite`), confirm neither the effective nor the selected account can be shared ("Switch away from … before linking it."). Run `codex` with `CODEX_HOME=~/.codex-test2` in a terminal, click "Link to the default account: …" on `test2`, check the modal text (it warns that resuming another ChatGPT account's session may be rejected) and confirm; "Codex is still running with account test2…" appears and nothing moved. Exit codex (and its daemon) and share again: the summary lists the moved and backed-up files; `sessions` and the other shared entries of `~/.codex-test2` are links, its rollouts are under `~/.codex/sessions`, its `history.jsonl` / `session_index.jsonl` lines were merged, `state_5.sqlite.independent-backup` (with `-wal` / `-shm` when present) stayed in the account, a differing `config.toml` / `AGENTS.md` is kept as `<name>.independent-backup`, `auth.json` and `memories` stayed, and no existing file in `~/.codex` was overwritten.
14. **Unlink a shared Codex account**: with the shared `test1` neither effective nor selected, click its "Unlink from the default account: …" button, confirm the modal (mentions that sessions, history and thread databases stay in the default account); with `codex` running as `test1` the busy message appears and nothing changes; otherwise the notification lists removed links and copied files; `~/.codex-test1` has no links left (including the dangling database links and `.tmp/rollout-maintenance.lock`), regular copies of `config.toml`, `AGENTS.md`, `hooks.json`, the config folders and the `skills` children, and no `sessions`, `history.jsonl`, `session_index.jsonl` or databases; `~/.codex` is unchanged; `auth.json` and `memories` stayed. Confirm the effective and the selected account are refused ("Switch away from … before unlinking it.").
